#include "GraphicElement/DynamicElement/Entity/PositiveEntity/PositiveEntity.h"
#include "Window/DynamicWindow/GameWindow/GameWindow.h"
int main(int argc, char * argv[]){
    GameWindow game_window(640, 480, "Game");

    while(game_window.display());//__print(1);
    

    // PositiveEntity object;
    // sf::RenderWindow window(sf::VideoMode(640,480), "game");
    // window.clear();
    // window.draw(object.sprite);
    // window.display();
    // std::cin.get();
    // window.close();

    
}